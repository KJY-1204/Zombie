# Plan

## 현재 상태 요약 (다른 컴퓨터에서 이어서 작업할 때 먼저 읽을 것)

- **최신 상태**: 프로젝트 방향이 크게 전환됨 — 기존 Slice 1~4(미니맵/레이더/인벤토리/상태창) + 근접무기 시스템은 그대로 유지되지만, **맵은 시골 묘지 콘셉트를 버리고 "도시→광산→방공호→시골" 4개 구역을 가진 대형 아포칼립스 맵**으로 재구축 중. 건물/터널/벙커는 입구 상호작용(E키) 시 별도의 내부 씬(Additive 로드)으로 이동하는 구조(Project Zomboid 스타일).
- **지금까지 완료된 것**: Slice 1~4 + 근접무기(기존 유지) → 맵 1차 확장(50x50 비례 확대, 폐기됨) → 시골 장식 2차(프리미티브, 폐기됨) → 시골 장식 3차(Blender, 폐기됨) → **씬 전환 시스템 + 도시 구역 1차 슬라이스(신규, 진행 중)**. 폐기된 시골/농장 콘텐츠도 에셋 파일 자체는 삭제하지 않았으므로(`Assets/Models/Level Art/*`, `Assets/Models/Rural/*`) 나중에 "시골" 구역 재구축 시 재사용 가능.
- **다음에 할 일**: 사용자가 "한 구역씩 순차적으로" 진행하기로 확정. 도시 구역을 더 확장(교차로/더 많은 건물/내부 템플릿 다양화)한 뒤 → 광산 → 방공호 → 시골 순서로 진행. 각 구역 완료 시 반드시 커밋 단위로 검증 후 사용자에게 보고.
- **새 세션에서 이어서 작업할 때 순서**: (1) 이 `plan.md`와 `context-notes.md`(특히 "대규모 맵 재설계 착수" 항목)를 먼저 읽어 씬 전환 아키텍처 결정 이유를 파악 → (2) `git log`/`git status`로 로컬 상태가 원격과 일치하는지 확인 → (3) Unity 에디터를 열어 컴파일 에러가 없는지, Play Mode에서 기존 기능 + 건물 진입/퇴장이 정상 동작하는지 베이스라인부터 재확인 → (4) 사용자에게 다음 작업 방향(도시 확장 계속 vs 광산으로 넘어가기)을 확인.

## 진행 중: 대규모 맵 재설계 — 씬 전환 시스템 + 도시 구역 1차
사용자가 "로우폴리 아포칼립스 좀비 맵(시골/도시/광산/방공호)"을 요청, "한 구역씩 순차적으로, 도시→광산→방공호→시골" 순서로 확정. 기존 묘지+농장 맵은 삭제. 건물/터널/벙커 입구 상호작용(E) 시 Additive 씬 로드로 내부 맵으로 이동하는 `SceneTransitionManager`/`BuildingEntrance`/`InteriorExit` 시스템을 신규 구현하고, House Interior 씬(단순 저폴리 방 1개)과 도시 구역 1차 슬라이스(70x70 지형, Blender 제작 저폴리 건물 10동 + 도로/인도 + 진입 가능 건물 1동)까지 검증 완료. 상세는 `context-notes.md` 참조.

## 폐기됨: Slice 6 — 맵 확장 (3차: Blender로 농장 소품 재제작, 시골 구역 재구축 시 참고용으로 보존)
2차(프리미티브 기반 농장 장식)가 "엉성해 보인다"는 피드백을 받아, 설치되어 있던 Blender 5.2.1을 헤드리스(`--background --python`)로 실행해 bpy/bmesh로 헛간(진짜 박공지붕)/사일로(원뿔 지붕)/나무(불규칙 수관)/건초더미(베벨)/목재울타리를 절차적으로 모델링 후 FBX로 내보내 Unity에 임포트, 기존 프리미티브를 전부 교체. 콜라이더+NavMeshModifier 재적용, NavMesh 재굽기+에셋 저장까지 검증 완료. 상세는 `context-notes.md` 참조.

## 완료: Slice 6 — 맵 확장 (2차: 시골/농장 테마 장식 추가, 프리미티브 버전 — 3차에서 교체됨)
1차(50x50 비례 확대)에 이어 사용자가 "시골느낌으로 꾸며달라" 요청. 프로젝트에 농장 전용 3D 에셋이 없고 AI 3D 생성 툴도 미설정이라, 기존 프리미티브(Cube/Cylinder/Sphere)로 헛간+사일로+목장울타리+건초더미+나무 11그루를 직접 구성해 확장된 외곽 영역에 배치(`Level Art/Rural Decor`). NavMesh 재굽기+에셋 저장, 헛간/사일로 물리 차단 검증 완료. 상세는 `context-notes.md` 참조.

## 완료: Slice 6 — 맵 확장 1차 (기존 묘지 마당을 50x50으로 비례 확대)
사용자가 "맵을 넓히자"고 요청(규모는 위임 → 2~3배/약 50x50, 기존 울타리 묘지 스타일 유지로 판단). `Ground`/`Fence`/`Fence Collider`를 동일 중심 기준으로 비례 확대, 좀비 스폰 포인트 재배치, 전체지도 카메라 범위 확장, NavMesh 재굽기+에셋 저장까지 완료·검증됨. 상세는 `context-notes.md` 참조.

## 완료: 근접무기(삽) 파지 자세 버그 수정 ("반대로 들고있다" 리포트)
사용자가 다른 컴퓨터에서 이어서 확인하던 중 "삽을 반대로 들고있다"고 지적. 원인은 회전 피벗이 손잡이 위치에서 멀리 떨어져 있어 회전 시 블레이드/손잡이가 뒤바뀐 것처럼 보이는 것이었음. 피벗을 손잡이 중간으로 재배치하고 대기/스윙 각도를 재조정해 해결. 상세 원인 분석/검증은 `context-notes.md` 참조.

## 완료: 근접무기(삽) + 무기 전환 시스템 (사용자 요청, 로드맵 외 추가 기능)

### 왜
사용자가 근접 공격으로 좀비를 잡고 싶다고 요청. 조사 결과 프로젝트에 근접 공격용 3D 모델/애니메이션 클립이 전혀 없어(사용 가능: Idle/Run/Walk/Reload/Aim Idle/Death뿐), 레벨 아트 소품인 `shovel.fbx`를 재활용하고 새 스켈레톤 애니메이션 없이 무기 자체를 스크립트로 회전시켜 스윙을 구현하기로 함(손은 기존 IK 구조로 무기 손잡이를 따라가므로 팔도 자연스럽게 따라 움직임). 총과 별개로 실제 무기 전환 시스템을 사용자가 요청함(Q키로 총↔삽 전환).

### 의존성 (Phase A 인스펙션으로 확인)
- `Gun.cs`/`GunData.cs`: 발사 로직과 데이터가 분리된 기존 패턴, `IDamageable.OnDamage()` 인터페이스로 데미지 적용.
- `PlayerShooter.cs`: `OnAnimatorIK`에서 `leftHandMount`/`rightHandMount`(현재는 PlayerShooter 자체의 고정 필드)의 월드 좌표로 양손 IK 강제 정렬 — 이 마운트 트랜스폼을 무기 쪽으로 옮기고 전환 가능하게 리팩터링 필요.
- `Player Character/Gun Pivot/Gun` 계층 구조: Gun Pivot이 매 프레임 팔꿈치 IK 힌트를 따라가고 Gun은 그 자식 — Melee Weapon도 형제로 배치하면 동일한 위치 추적을 공짜로 얻음.
- `shovel.fbx`: 메시 바운드 (0.38, 0.06, 1.56) — 이미 실제 크기라 리스케일 불필요.
- Zombie 루트는 이미 `Enemy` 레이어(Slice 1) — 근접 판정에 재사용.

### 완료 기준 (Acceptance) — 검증 완료(checklist.md 참조)
- Q키로 총↔삽 전환, 좌클릭이 현재 장착 무기에 맞는 동작(발사/스윙) 수행. ✅
- 스윙 시 무기가 실제로 호를 그리며 움직이고, 판정 범위 내 좀비에게 데미지 적용. ✅
- 기존 총 발사/재장전 회귀 없음. ✅

## 완료: Slice 4 — 플레이어 상태 창 (Project Zomboid 스타일)

### 왜
Phase A 인스펙션 중 발견: 체력 UI(발밑 방사형 링)가 이미 존재해 CLAUDE.md의 "기존 체력을 authoritative source에서 보여주기" 요구는 원래 충족돼 있었다. 사용자가 이를 확인한 뒤, 발밑 링 대신 Project Zomboid처럼 `I` 키로 여닫는 상태 창으로 바꿔달라고 요청해 방향이 바뀜.

검증 완료(checklist.md 참조). 이후 사용자 요청으로 실루엣+세로 체력바 스타일 개선, 색상 연동(체력 낮으면 붉게), 아이콘 위치 버그 수정까지 완료.

## 완료: Slice 3 — 인벤토리 foundation (체력팩 + 레이더)
검증 완료(checklist.md 참조).

## 완료: Slice 2 — 레이더로 좀비 임시 표시
검증 완료(checklist.md 참조). 이후 사용자 요청으로 RadarPack 비주얼을 구체→탐지기 모양(받침대+안테나+접시)+배색(회색 본체+초록 접시)으로 개선.

## 완료: Slice 1 — 원형 미니맵 (좀비 미표시)
검증 완료(checklist.md 참조). 이후 사용자 요청으로 마우스 조준+WASD 이동 컨트롤 변경, 스트레이프 애니메이션 개선, M키 전체 지도 창(시작 시 1회 촬영하는 정적 "종이 지도")까지 추가됨(미니맵/상태창과 별개, context-notes.md 참조).

## 다음 마일스톤 (순서대로, 근접무기 완료 후 — 사용자 확인 후 시작할 것)
Slice 5 차량 → Slice 6 월드 확장.
